# Agent Builder (WordPress.org free tier)

`renduples/agent-builder-wp` — the free/WordPress.org-facing repo for **Agent Builder**, a multi-agent AI orchestration plugin for WordPress. Live test/dev deployment is **lffci.org** (Lets Farm Financial Cooperative, Inc.), reachable via `ssh lffci`.

## Current status (as of 2026-08-19)

- Git HEAD and lffci.org are in sync at **3.3.81**, pushed to `origin/main` through 3.3.75 (3.3.76–3.3.81 are local-only as of this writing).
- **Not yet released to WordPress.org SVN.** The last SVN release marker is `2235d8a` "Release free (WPorg) v3.3.46" (2026-08-03). Everything from 3.3.47 through 3.3.81 — 35 versions — is committed and deployed to lffci.org, but never pushed to the public WordPress.org listing. Worth doing before too much more accumulates.
- **3.3.81 — MCP gets a Settings tab.** The plugin's own MCP JSON-RPC server (`class-relay-connect.php`) has existed since well before this version but had zero UI anywhere — the only trace was an orphaned `doc-map.php` entry pointing at a non-existent tab, which is what confirmed a dedicated tab (not folded into Endpoints) was the original intent. New "MCP" tab in the Advanced settings group (`includes/class-admin-settings-rest.php`'s `data_mcp()`, wired into `get_bootstrap()`/`register_routes()`; React `MCPTab` in `src/settings-app/index.js`): an availability card (Pro/connector/`can_use` status), a table of every active agent's own `/{slug}/mcp` URL with copy-to-clipboard and a live "Test" button (`POST .../mcp-test` → `Agentic_Relay_Connect::count_agent_tools()`, reusing the exact fail-closed `mcp_readiness()` logic added for this), a connected-clients list, and full Application Password credential management scoped to the `Agentic_Relay_Connect::APP_PASS_NAME` ("Agent Builder Relay") credential — including in-tab **creation** (`POST .../mcp-create-credential`, plaintext shown once, matching WP core's own Application Passwords screen convention), not just list/revoke, per explicit user choice over the more conservative list-only option. Revoke (`POST .../mcp-revoke-credential`) is gated by a new, deliberately stricter `can_manage_mcp_credentials()` (`manage_options` only, not the general `agentic_manage_settings`/`can_manage()` settings-edit capability) since it can revoke another administrator's credential. All three routes sit outside the tab's save cycle (no editable fields to submit), same pattern as the existing Endpoints tab's `test-service` route. `Agentic_Relay_Connect` gained two small public wrappers reused by the new REST handlers rather than duplicated: `mcp_readiness( $slug )` (promotes the existing private fail-closed check to return `{ready, reason}` instead of just bool, so the UI can explain *why* an agent isn't exposing tools) and `count_agent_tools( $slug )`.
  - **Verified data-layer, end-to-end, via WP-CLI** (not via live browser — the Chrome extension wasn't connected this session): `data_mcp()` correctly lists all 10 active bundled agents with real, correct MCP URLs and `ready: true`; `mcp_test('user-assistant')` returned a real tool count (13) matching the live scoped-per-agent tool list; `mcp_create_credential()` minted a real Application Password that `WP_Application_Passwords::get_user_application_passwords()` confirmed was actually stored under the `Agent Builder Relay` name; `mcp_revoke_credential()` then confirmed-removed it from that same store. `debug.log` clean throughout (only stale pre-existing entries from earlier in the day, nothing new). **Worth a live in-browser click-through next session** to confirm the React rendering itself (card layout, copy button, plaintext-password notice) has no surprises — the data path underneath it is solid.
- **3.3.80 — Scoped MCP tools to each agent's own manifest.** The MCP route is per-agent (`/{slug}/mcp`) but the handler ignored the slug entirely, exposing the identical full tool set through every agent's endpoint regardless of what that agent's own `abilities.json` actually declared (e.g. Skills Assistant's MCP endpoint could call `manage_user_privileges`, a User Assistant-only tool). Threaded `$slug` through the whole listing/execution call chain in `class-relay-connect.php`, gating both on `Abilities_Manifest::is_declared($slug, $tool_name)` — the same fail-closed philosophy chat already used.
- **3.3.79 — MCP fallback for WordPress 6.4–6.8; closed a live unfiltered-tool-exposure gap.** The plugin's MCP server was hard-wired to WP core's native `wp_get_abilities()`/`wp_get_ability()` (6.9+ only), so it was completely non-functional below 6.9 despite the plugin's own declared "Requires at least: 6.4". Built a WP-version-independent fallback via the plugin's own `Tool_Loader`/`Tools_Registry`. While building it, found the **existing** native-Abilities MCP path had zero risk-tier awareness — any registered ability, including `run_wp_cli`, was fully listable and callable via MCP with no filtering — and fixed that on both the new fallback and the pre-existing native path via a shared `is_tool_mcp_safe()` block-list/risk-tier check.
- **3.3.78 — Trimmed readme.txt's changelog** back down to the ~10 genuinely major releases (was ~78 entries/53KB, the same bloat 3.3.29 had already cut once before).
- **3.3.77 — Two WP.org Plugin Check findings fixed**: `Approval_Queue::ksort_recursive()` used `array_is_list()` (native PHP 8.1+, but WordPress only polyfills it since core 6.5 — one version past this plugin's declared 6.4 floor) — replaced with a manual `is_list()` helper; readme.txt undercounted bundled agents as "eight" in four places (missed Agent Orchestrator and Skills Assistant) — corrected to ten, and added the missing External Services disclosure.
- **3.3.76 — User Assistant gets role/privilege management; Settings > Users gets the Basic/Advanced split.** New tool `manage_user_privileges` wraps the existing `User_Roles`/`Usage_Limits` data layer (`get`/`set_privilege`/`set_usage_limit`/`set_anonymous_chat` — the same settings the classic Settings > Users tab manages), plus a new `user-privilege-management` Skill. Risk: `high` for privilege grants/revokes (a real trust decision, matching `manage_cli_settings`'s precedent), `low` for usage limits, `medium` for anonymous chat, `none` for reads — all via `abilities.json` `risk_by_action`, **deliberately not** a `BASELINE_RISKS` floor (see gotcha below). Settings > Users now has its own Basic/Advanced split — same pattern as Skills/Tools/Approvals/Activity, but scoped to one Settings *tab* via its own screen key (`'settings-users'`) rather than a whole top-level page — `Admin_Menu_Handler::is_advanced_mode()` already supported this generically, no change needed there. Basic mode embeds a chat with User Assistant instead of the raw role/privilege matrix.
  - **Refactor**: extracted the chat-embed (`ChatEmbed`/`ProposalCard`) and `ScreenModeToggle` components Skills introduced in 3.3.75 out of `src/admin-pages/index.js` into new `src/shared/chat-embed.js` and (for the toggle) `src/shared/components.js`, since Settings' Users tab is a second genuine consumer — avoids a third near-identical chat-UI implementation. `admin-pages`'s existing Skills embed now consumes the same shared components; regression-checked live post-refactor (Basic + Advanced Skills both still render correctly, no console errors).
  - **`BASELINE_RISKS` gotcha found while designing this** (not fixed — flagging for later): `Risk_Level::get_tool_default( $tool_name )` takes no `$call_action` parameter, so a `BASELINE_RISKS` floor (`class-risk-level.php`) applies flat across every action of a tool, not per-action. `manage_cli_settings => HIGH` (added in the 3.3.74 mirror commit) therefore silently overrides its own agent's `risk_by_action: {"get_whitelist": "none"}` downgrade from 3.3.71 back up to HIGH via `Abilities_Manifest::get_effective_risk()`'s `max(tool_default, manifest_risk)` — reintroducing the exact "read action needs approval" bug 3.3.71 fixed, but only for that one tool. Deliberately did *not* add a `BASELINE_RISKS` entry for `manage_user_privileges` to avoid the same trap — its risk floor is `none` (unregistered tools default there) and the abilities.json's flat `high` + `risk_by_action` map is the sole source of truth, matching Skills Assistant's already-verified-working approach.
  - **Two real bugs found during verification, both fixed**:
    1. Settings' render path (`Admin_Menu_Handler::render_settings_page()`) never enqueued `chat.css` at all — a separate code path from the admin-pages React screens, which already load it. The new chat embed rendered completely unstyled (no border, no avatar, plain text) until this was added.
    2. A tool file added to an *existing, already-active* bundled agent doesn't reach the LLM until `Tool_Loader`'s tool-path list (cached in a 24h transient, `agentic_tool_paths`) is busted — `sync_to_registry()` only runs from the Tools admin page's lazy loader or the `Activator` upgrade cascade, neither of which fires just from editing `agent.json`/`abilities.json` on disk. Confirmed live: `manage_user_privileges` was silently absent from the LLM's tool list (the model apologized that it "wasn't accessible") until either was triggered. Bumped `AGENT_BUILDER_DB_VERSION` (2.13.3 → 2.13.4) specifically to guarantee this on every existing install after update, not just lffci.org — this is a genuinely new scenario for this codebase (every prior new-tool-file commit also happened to add a new bundled agent, which already forced a DB bump for unrelated reasons, so this gap was never hit before).
  - Verified end-to-end live in-chat: asked User Assistant "Which roles can currently access plugin settings and chat with AI agents?" (correct, tool-backed answer, no confirmation needed — `get` is risk `none`), then "Let editors view the audit log." → confirmed in plain language → high-risk **Needs Your Approval** card rendered correctly → Approve → "✅ Done — change applied." → confirmed server-side the privilege was actually granted, then reverted the test change. `debug.log` clean throughout.
- **3.3.75 — Skills gets the Basic/Advanced split, via a new bundled Skills Assistant agent.** Same precedent as Publish/Agent Orchestrator (3.3.67): Basic mode embeds a chat instead of the raw SKILL.md create/edit forms. New agent `skills-assistant`, two new tools (`manage_skill` — thin CRUD wrapper over `Skills_Registry`; `browse_community_skills` — WordPress.org repo + Agentic's curated feed, matching Basic mode's existing source restriction), and a new `skill-management` Skill teaching the agent's own tools. Two surfaces both needed it: classic-PHP `admin/skills.php` (new `admin/skills-basic.php`, reuses `chat.js`/`chat-interface.php` exactly like Publish) and the separate React `SkillsView` actually reached from the nav sidebar (new `SkillsChatEmbed`/`ProposalCard` in `src/admin-pages/index.js`, following `editor-sidebar.js`'s self-contained-hooks-hitting-`agentic/v1/chat`-directly pattern rather than trying to reuse `chat.js` in React — confirmed via research that `chat.js`'s single-boot-on-DOMContentLoaded design and `chat-interface.php`'s live-PHP-object markup don't graft onto React cleanly). `ProposalCard` ports `chat-overlay.js`'s once/session/always/deny + approve/reject card logic onto the same `agentic/v1/proposals`/`agentic/v1/approvals` endpoints. Existing sites activate it via the standard `maybe_upgrade()` DB-version-bump cascade (2.13.2 → 2.13.3), same as every prior bundled-agent rollout.
  - **Bug caught and fixed during verification**: `manage_skill`'s `create`/`update` let the frontmatter parser's slug-shaped `name:` field (spec-mandated — `validate_spec_fields()` requires `^[a-z0-9]+(-[a-z0-9]+)*$`) silently override the human-readable display name the LLM was explicitly given, so asking for a skill called "Test Ping Reply" produced a skills-list entry literally named "test-ping-reply". Explicit `name`/`description` args now win; the parsed frontmatter is only authoritative when the caller passes a complete pre-made SKILL.md with no separate structured fields (same fix applied to the `validate_spec_fields()` calls in `do_get()`/`do_create()`/`do_update()`, which had the same mismatch — now validate the frontmatter-shaped name, not the DB display name).
  - **Bigger find while deploying**: lffci.org had never received the previous mirror commit, 3.3.74 ("fix: close port-review items"), before this session — 18 files across `includes/` and `library/tools/` were stale on the live site. Deploying them surfaced that **3.3.74 itself shipped broken**: `class-llm-client.php`'s new tool-stripping/retry logic called three `Model_Capabilities` methods (`supports_tools()`, `is_tools_unsupported_error()`, `mark_tools_unsupported()`) that were never actually added to that class in the same commit — a fatal error on *any* chat request that included tools, i.e. effectively all of them. This means **3.3.74 was fatally broken for every agent on every site running it**, not just lffci.org, from the moment it was deployed anywhere. Implemented all three (reads the existing `supports_tools` capability flag; the other two are a runtime-learning pair — pattern-match a provider's tool-rejection message, remember it per model/provider via a new `agentic_learned_tools_unsupported` option so future requests strip tools up front). Fixed in `includes/class-model-capabilities.php`, its own commit (`f8f4ee2`) ahead of the Skills Assistant one so the fix reads clearly as independent of the new feature.
  - **Verification caveat**: agentic-plugin.com's own hosted LLM relay (the `agentic` provider, lffci.org's configured default — confirmed via a direct WP-CLI `Agent_Controller::chat()` call against the long-established WordPress Assistant, not just the new agent) was returning HTTP 500 for the entire ~25-minute verification window, so a full live in-chat round-trip (create-via-chat → see it in the list → browse/import → core-skill-edit confirmation card) wasn't completable this session. Everything reachable without the LLM was verified directly instead: both tools round-tripped correctly via direct WP-CLI invocation (create/get/update/list/delete; browse/import against the real WordPress.org repo), the outgoing request body logged to `debug.log` showed correctly-assembled system prompt/tools/`deployment_context`, and both UI surfaces (classic-PHP and React) were confirmed live in-browser — Basic mode swaps cleanly to the chat with no console errors, no double-boxing, Basic/Advanced toggle in the standard top-right spot, `debug.log` clean of anything but the external 500s. Worth a live chat re-check once the relay recovers.
- **3.3.73 — Media Library's "Ask AI" launcher was invisible on the screen almost everyone lands on.** Found via general Agent Orchestrator testing (asked "which wp admin screens have launchers," it correctly listed Media as enabled — user reported "I don't see it at media"). `includes/class-admin-surfaces.php`'s Media implementation was structurally different from the other four contextual-launcher screens from the start: Dashboard/Plugins/Users/Comments all get an always-visible toolbar-level launcher, but Media only had `attachment_fields_to_edit` (fires inside an individual image's edit modal — requires clicking into an item first) and `media_row_actions` (fires only in List View, a manually-toggled alternate mode WordPress doesn't default to — the default is Grid View). Neither is visible on first landing on Media Library. Two-part fix: (1) added `media_toolbar_launcher()` on `restrict_manage_posts` (scoped to `post_type === 'attachment'`, since that hook is shared across every post-type list table) to cover List View, matching the existing `restrict_manage_users` pattern for Users; (2) Grid View needed something entirely different, since it's rendered client-side by Backbone (`wp.media`) and no PHP action fires inside it at all — added `media_grid_launcher_js()` on `admin_footer-upload.php` that injects the identical launcher HTML via JS. First attempt (a one-time `querySelector`-and-append at script-parse time) silently lost a timing race against Backbone's own async render of that toolbar region, which clears and repopulates the container — the launcher would insert, then get wiped a moment later with no error. Fixed with a `MutationObserver` on `document.body` (not the toolbar node itself, since Backbone may replace that element outright rather than mutate it in place) that re-inserts on every DOM change, idempotent via an existing-launcher check. Verified live end-to-end: launcher visible immediately in default Grid View, click opens the chat overlay with WordPress Assistant and the correct pre-filled prompt, confirmed no leak onto Posts/Pages (`restrict_manage_posts` fires for every post type, so the `attachment`-only guard is required), `debug.log` clean.
- **3.3.72 — `seo-assistant` typo, live since the plugin's very first commit, finally fixed.** Prompted by "verify what the Agent Orchestrator reports matches what the classic Advanced tabs show, and vice versa" — cross-checked its "where are agents currently deployed?" answer against all 8 classic Publish tabs one by one. 7 of 8 matched exactly (Shortcodes, Frontend Modal, Gutenberg Blocks, Scheduled Tasks, Event Listeners, Admin Bar, Admin Bar Launcher — CLI wasn't mentioned by the agent at all, but was genuinely empty too, so a completeness gap, not an inaccuracy). The 8th, Editor Sidebar, was **wrong on both sides identically** — both correctly reflected the same corrupted underlying data, so Basic and Advanced were technically consistent with each other, just consistently wrong. Root cause: `Activator::set_default_options()`'s fresh-install default for `agentic_editor_sidebar_settings` has hardcoded the SEO agent's slug as `'seo-assistant'` since `23fcdc2`, the very first commit in this repo's history (`git log -S"seo-assistant"` confirms) — the real slug is `seo-optimizer`. Since it's the fresh-install *default*, and the last public WordPress.org release (v3.3.46) postdates that first commit by a wide margin, this almost certainly affects real installed sites, not just lffci.org. The bad slug had propagated to **two** separate storage locations that both needed fixing: the `agentic_editor_sidebar_settings` option itself, and a `wp_agentic_deployments` row (type `admin_ui`) that `Deployments_Migrator` had copied it into verbatim the first time it ran — the row is the one the classic Editor tab and the `manage_editor_sidebar_agent` tool actually read once any rows exist, so fixing only the option (my first pass) visibly changed nothing. Fixed both: the fresh-install default itself, a new idempotent one-time migration (`Activator::migrate_editor_sidebar_seo_slug()`, gated on its own flag option, wired into the existing `run_migrations()` — same place `migrate_agent_personas()`/`migrate_agent_overrides()` live) that corrects the option and either renames or drops-as-duplicate the bad Deployments row, and triggered it for existing installs the established way: bumping `AGENT_BUILDER_DB_VERSION` (2.13.1 → 2.13.2) so `Activator::maybe_upgrade()`'s cascade reaches it. Verified live on lffci.org at every layer — the option, the Deployments row, and `manage_editor_sidebar_agent`'s own tool output all now correctly say `seo-optimizer`, and the classic Editor tab checkbox renders checked. **Not independently re-confirmed via a fresh live chat response** (browser input became unreliable for several exchanges in a row this session — a known recurring extension-connection issue, not evidence of anything wrong with the fix) — the data-layer verification is conclusive on its own, since that's the actual surface every consumer (chat tool, classic tab, REST) reads from, but the LLM's prose synthesis of the corrected data wasn't watched rendering live.
- **3.3.71 — Agent Orchestrator's tools were unusable for read-only questions; new project-wide `risk_by_action` capability.** User report ("where are agents currently deployed?" — a purely informational question) surfaced two stacked bugs, both from live chat-driven testing:
  1. **Root cause**: all 8 Agent Orchestrator tools required in-chat confirmation for *every* call, including pure `list`/`get` lookups — risk was declared per-tool-name in `abilities.json`, and `Tool_Executor::execute()`'s risk gate (`Abilities_Manifest::get_effective_risk()`) never looked at which `action` was actually being called. Asking to see current deployments made the LLM call all 8 `manage_*` tools and get 8 separate approval prompts. Fixed by extending the risk system, not just this feature: `abilities.json` entries can now declare an optional `risk_by_action` map (e.g. `{"risk": "low", "risk_by_action": {"list": "none"}}`) that `get_declared_risk()`/`get_effective_risk()` consult when a `$call_action` is passed. Preserves the existing, documented invariant exactly — "agent manifests can only escalate risk, never downgrade it below the tool's own intrinsic default" — because the final result is still `max(tool_intrinsic_default, resolved_manifest_risk)`; this only works at all because all 8 tools' own registered intrinsic default in `wp_agentic_tools` was already `none` (confirmed via DB query before implementing), so `risk_by_action` narrowing the manifest side down to `none` for reads correctly resolves to `none` overall, while writes still floor at the tool's declared `low`/`medium`/`high`. `Tool_Executor::execute()` now passes `$arguments['action']` through to the risk gate. Available to any agent/tool going forward, not just Agent Orchestrator.
  2. **Compounding display bug**: when multiple tool calls in one turn each needed confirmation, the response text and the "Proposed Change" card described two *different* proposals (last-seen vs first-seen, from two loops in `class-agent-controller.php` that disagreed) — confusing even before bug 1 existed, and would recur for any future multi-write turn. Root cause: a code comment already said "stop looping as soon as a tool needs user sign-off," but the loop didn't actually `break` — it kept executing (and creating proposals for) every remaining tool call in the batch. Added the missing `break`.
  - Both verified live end-to-end post-fix: the same "where are agents currently deployed?" query now returns a complete, direct, correctly-worded answer with zero confirmation prompts.
- **3.3.70 — Editorial Director can now find thin posts, not just count them.** Follow-up to the 3.3.69 truncation bug: the underlying reason Editorial Director's answer needed 8 tool iterations (and blew its token budget) in the first place was that it only had `get_content_stats` (returns an aggregate `thin_post_count`, no list) — it had no tool that could tell it *which* posts were thin, so it resorted to fetching posts itself and reasoning it out. Rather than build a new tool, found `list_posts_needing_seo` (already used by SEO Optimizer) already supports `issue: "thin_content"` with the identical `<300 words` threshold `get_content_stats` uses, returning `{id, title, word_count, url}` — just granted it to Editorial Director's `agent.json`/`abilities.json` (`risk: none`, mirroring SEO Optimizer's existing classification). Note: the tool's `post_type` param is a single value defaulting to `post`, not `post|page` together like `get_content_stats` scans — the model correctly worked around this live by calling it twice (once per type) to get the complete, accurate list (1 thin post + 29 thin pages = 30, matching the aggregate count exactly).
  - **Operational gotcha hit while deploying this**: editing a bundled agent's `abilities.json` directly and deploying it breaks that agent immediately — `Abilities_Manifest::verify_integrity()` checks the file's HMAC against a `abilities-signature` file in the same directory (server-local, generated at activation time via `save_integrity_hash()`, never committed to git), and a content change with no matching signature update fails closed: the agent gets fully blocked with "Manifest signature mismatch" until fixed (audit log: `integrity_failure`; chat shows a generic "temporarily disabled due to a security check" error). The fix is *not* necessarily the deactivate/reactivate the error message suggests (disruptive — cycles every agent) — call `\Agentic\Abilities_Manifest::save_integrity_hash('<slug>')` via `wp eval` for just the one agent right after deploying its updated `abilities.json`. **Any future edit to any bundled agent's `abilities.json` needs this same re-sign step as part of the deploy, every time** — easy to forget since nothing in the deploy workflow currently calls it out, and the DB-only tables (`wp_agentic_agent_library` etc.) don't reflect it either.
- **3.3.69 — Truncated LLM responses were silently shown as complete.** User report: Editorial Director's answer to a tool-heavy multi-step request ("Find thin posts, then coordinate rewriting and SEO improvements") stopped mid-sentence right as it was about to write out a list, with no error and no indication anything was wrong. Root cause traced via the audit log (`chat_complete` details showed 8 tool iterations, response ending exactly at "Here's the list of your published posts:") to Gemini returning `finishReason: MAX_TOKENS` on the final completion — the model's own "thinking" tokens across an 8-iteration tool loop evidently ate the output budget before the visible answer was written. `includes/class-llm-client.php`'s **non-streaming** Google/Anthropic response parsers already correctly mapped this (`normalize_google_response()`'s `MAX_TOKENS → 'length'`, `normalize_anthropic_response()`'s `max_tokens → 'length'`), but the **streaming** parsers (`parse_google_stream_chunk()`, `parse_anthropic_stream_chunk()`) — the actual path the interactive Chat page uses — never read the provider's finish/stop reason at all, so streamed truncation was invisible even in principle. Fixed both streaming parsers to capture and map it identically to their non-streaming counterparts (careful not to override an already-detected `'tool_calls'` finish, since Gemini can report `STOP` on the same chunk that carries the functionCall). `includes/class-agent-controller.php`'s `chat()` (interactive) and `run_autonomous_task()` (scheduled tasks) now check for `finish_reason === 'length'` at the point they accept the model's final text and append a visible truncation notice rather than presenting the fragment as complete — confirmed via `WP_REST_Server`'s SSE `'end'` event, whose `response` field fully overwrites the streamed bubble content client-side (`assets/js/chat.js`), so the notice reaches the live chat UI immediately, not just on reload. **Scope note**: `includes/class-agent-job-processor.php` (the separate, narrower agent-creation-wizard background job, `agent_id: 'assistant-trainer'`) has the same class of gap — never reads `finish_reason` at all — but was left out of this pass as a distinct, lower-frequency flow; worth the same treatment later. **Verification gap**: the exact truncation from the original report could not be reliably reproduced live afterward (LLM output length is non-deterministic, two follow-up attempts — including one deliberately engineered to demand a very long answer — both completed without truncating) — the fix is verified at the code level (traced against the documented provider finish-reason semantics and the existing, already-correct non-streaming mapping) and deployed, but not confirmed by observing the actual warning render live.
- **3.3.68 — Dashboard Quick Actions: user-picked actions were silently invisible.** User report: checked "Tools" in the Dashboard's "Manage Actions" picker, saved, and it never appeared as a button. Confirmed the save itself was correct (`agentic_quick_actions_enabled` user meta already had `tools` in it) — the bug was purely a client-side render filter in `src/dashboard-app/index.js` (`QuickActionsCard`): any catalog entry flagged `advanced: true` (Tools, Skills, Approvals, Publish, Settings) was filtered out of the rendered button rows unless `data.is_advanced` (`Admin_Menu_Handler::is_advanced_mode('dashboard')`) was also true — i.e. a personal, explicit per-user selection was being silently overridden by an unrelated global/screen Basic/Advanced setting, with no UI explanation why. Same root principle as 3.3.64 ("Basic/Advanced should only ever affect a page's own content, never navigation/what's reachable") — removed the `(!a.advanced || data.is_advanced)` condition from both the `primary` and `secondary` filters so `enabled` alone (the user's actual choice) is authoritative; the "Advanced" note next to those items in the picker itself stays as an informational label. Verified live on lffci.org: Tools now renders correctly in the Quick Actions row while the Dashboard is still in Basic mode.
- **3.3.67 — Publish gets a Basic/Advanced switch; new bundled agent, Agent Orchestrator.** Publish was the last screen missing the Basic/Advanced content split the other four screens got in 3.3.63. Advanced mode is the unchanged 8-tab classic UI (Admin bar, CLI, Editor, Event Listeners, Frontend Modal, Gutenberg Blocks, Scheduled Tasks, Shortcodes). Basic mode embeds a chat with a new bundled agent, **Agent Orchestrator** (`library/agents/agent-orchestrator/`), fully tool-backed for all 8 areas via 8 new tools in `library/tools/` (`manage_agent_shortcode`, `manage_frontend_modal_agent`, `manage_gutenberg_block_agent`, `manage_editor_sidebar_agent`, `manage_admin_bar_launcher`, `manage_scheduled_task`, `manage_event_listener`, `manage_cli_settings`) plus a new Skill (`library/skills/agent-deployment/SKILL.md`) documenting the workflow per channel. Scheduled-task and event-listener CRUD (the two with real side effects beyond a `Deployments` row write — WP-Cron scheduling, dual-write) were extracted out of `Admin_Ajax` into shared `Agent_Lifecycle` static methods so the classic AJAX handlers and the new tools call the same code, not duplicates. The chat embed reuses the existing dedicated Chat page's template (`templates/chat-interface.php` + `assets/js/chat.js`) via a new `$agentic_force_agent` var that suppresses the agent-switcher and hard-codes Agent Orchestrator — not the `[agentic_chat]` shortcode path. New sites get Agent Orchestrator active from first install as usual; existing sites get it via the standard `Activator::maybe_upgrade()` cascade, triggered by bumping `AGENT_BUILDER_DB_VERSION` (2.13.0 → 2.13.1) — no new migration code needed, this is the established, sanctioned mechanism for shipping anything that must auto-apply post-update without a manual re-activation.
  - **Two real bugs found and fixed during live chat-driven verification** (drove `manage_agent_shortcode` through actual chat on lffci.org rather than only checking each tool's PHP in isolation):
    1. `assets/js/chat.js`'s tool-approval flow has a generic convention (pre-dating this feature, used by `create_form`/`list_forms`/etc.): if an approved tool's result has a top-level `shortcode` key, it's rendered verbatim in backticks as "paste this shortcode" copy — the convention expects a plain string. `manage_agent_shortcode`'s `do_create()`/`do_update()` returned a *nested object* under that same key name, so the chat literally rendered `[object Object]` instead of the shortcode. Fixed by renaming the nested summary to `deployment` and adding a plain-string `shortcode` key alongside it, matching the existing convention instead of colliding with it.
    2. Far more serious: `Deployments::save()` (`includes/class-deployments.php`) rebuilds every DB column from only the keys present in the `$data` array passed to it (via `prepare_row()`) — it was never a true partial update. Any caller that passes a subset of fields on an *update* (both the classic Edit form for shortcodes and every one of the new tools do this — none of them re-send `type`, `page_id`, or `created_at`) silently blanks the omitted columns: `type` gets reset to `''` (which drops the row out of every classic tab's listing, since those filter `WHERE type = ...`), and `created_at` gets overwritten to "now" on every single edit. Caught by renaming a test shortcode via chat and finding the classic Shortcodes tab still showed the old label — traced through the audit log, then confirmed directly via `wp eval` calls to `Deployments::save()` bypassing the LLM entirely. Fixed in `save()`: on update, fetch the existing row and `array_merge()` it under the incoming `$data` *before* calling `prepare_row()`, so omitted fields survive. This bug predates this session's new tools — the classic Shortcodes tab's own Edit button has always had the same partial-update pattern and would have hit it too; nobody had apparently exercised Edit enough to notice `type` silently going blank.
  - **Verification gap**: only 2 of the 8 tool-backed areas (`manage_agent_shortcode`'s create/update, `manage_frontend_modal_agent`'s enable/re-enable) were actually round-tripped through chat and cross-checked against the classic tab; the browser extension used for testing repeatedly dropped its tab-group connection mid-session (lost the logged-in WordPress session twice, required manual re-login), so the remaining 6 tools (`manage_gutenberg_block_agent`, `manage_editor_sidebar_agent`, `manage_admin_bar_launcher`, `manage_scheduled_task`, `manage_event_listener`, `manage_cli_settings`) were only exercised by reading their PHP, not by driving them live. Given the `Deployments::save()` fix is shared infrastructure (not per-tool), and the two verified tools both hit it correctly post-fix, the remaining 6 are very likely fine — but haven't been proven live the way the plan's verification step called for.
- **3.3.66** — removed the redundant "Community Agents" page-title-action button next to "Upload Agent" on the Agents list (`admin/agents.php`); the same link stays in the filter row below (All | Active | Inactive | Community Agents).
- **3.3.65 — "assistant" → "agent" terminology standardization.** Product decision: 6 months ago "assistant" was the more familiar word for non-technical users; "agent" is well understood now, so the whole UI should say "agent" consistently. Scope rule that resolved every ambiguous case: a specific bundled agent's *actual name* never changes (WordPress Assistant, Assistant Trainer, User Assistant keep their real names everywhere — chat headers, dashboard links, delegation routing, README feature list), but generic wording describing the concept always changes to "agent". Also explicitly out of scope and untouched: the LLM chat-protocol role literal `'assistant'` (OpenAI-style message role, appears throughout `class-agent-controller.php`/`class-llm-client.php`/etc.), directory/slug identifiers (`wordpress-assistant`, `assistant-trainer`, `user-assistant`, ...), and `agent.json` `name` fields.
  - Roughly 150 string edits across 57 files: PHP translatable strings (admin pages, deployment surfaces, settings tabs, REST payload copy, email/notification text), React JS (`src/admin-pages`, `src/settings-app`, `src/agent-wizard`), plain frontend JS assets (`assets/js/chat-overlay.js`, `editor-sidebar.js`, `gutenberg-blocks.js` — these are outside the wp-scripts build, easy to miss if you only grep `src/`), `readme.txt` (generic lines changed, bolded agent-name feature-list entries and historical changelog entries left alone), and one `agent.json` **description** field (`library/agents/assistant-trainer/agent.json` — descriptions are *not* names, they're rendered UI copy on the Agents list page, unlike `name` fields which must stay put).
  - Executed as parallel background-agent sweeps per file cluster (each given the exact leave-alone rules plus a strict "if a string's status is ambiguous, skip it and report back" instruction) with the trickiest name-vs-copy files (`admin/setup.php`, `includes/class-dashboard-rest.php`, `includes/class-manifest-agent.php`, `library/tools/generate_agent/tool.php`, `library/tools/create_agent_files/tool.php`) handled directly rather than delegated. Verified with a full-repo `grep -rn "ssistant"` sweep afterward, categorized every remaining hit, and found (then fixed directly) two gaps the initial file-list missed entirely: `admin/train-data.php`, `admin/deployment.php`, and the `assets/js/*.js` plain-JS files.
  - **Follow-up not done in this pass**: existing translations across the 11 supported locales are now stale for every changed string (the old English string a translation was keyed to no longer exists, so translated UI will silently fall back to the new English text until someone retranslates). Regenerating the `.pot`/re-translating is a separate, larger task — see the i18n note below for the regen command; actual translation into 11 languages wasn't attempted here.
- **3.3.61 visual verification: done.** Confirmed live on lffci.org via browser click-through: Basic mode shows only Skill/Agent/Actions columns; Advanced mode adds the Source badge ("Core"), Version column, and Export action, exactly as the commit intends. No regressions found.
- **3.3.62 — fixed the Settings save bug found during that verification.** Root cause: `SaveBar` (`src/shared/components.js`) did `<Button onClick={ onSave } ...>`, so WordPress's `Button` passed the raw click event straight through as `onSave`'s first argument. The top-level `onSave` in `src/settings-app/index.js` treats that argument as an `overrideData` object and spreads it into the save payload (`{ ...(overrideData || data) }`) — spreading a React synthetic event pulls in circular fiber/DOM references, so `apiFetch`'s internal `JSON.stringify()` threw before any request was sent, and the tab's state silently reverted on reload. Affected every Settings tab that wired `SaveBar` directly to the shared `onSave` — Interface, Security, Memory, Agents, Instructions (Users and Endpoints already worked around it locally with a no-arg wrapper, `() => onSave()`). Fix: `SaveBar`'s button now calls `() => onSave()`, one line, fixes all five tabs at once. Verified live on lffci.org: toggled Interface mode to Advanced, saw "Settings saved.", reloaded, setting persisted; reverted back to Basic afterward. `debug.log` clean (only a pre-existing, unrelated Rank Math SEO warning).
- **3.3.63 — standardized the Basic/Advanced switch, fixed dead batch-approve, fixed table width.** Three related fixes from a browser-driven pass over Tools/Skills/Approvals/Activity, the four screens with a Basic/Advanced content split:
  - **Standardized switch placement.** The switch previously lived inline in body-copy paragraphs, worded differently per page, and Skills had no switch at all (silently inherited the global default with no way to override it per-screen). Moved into `AdminPage`'s existing `actions` slot (top-right), same spot on all four screens now. `ScreenModeToggle` is rendered once, centrally, in `AdminPagesApp` — see `SCREENS_WITH_MODE` in `src/admin-pages/index.js`.
  - **Approvals bulk approve was fully dead, not just hard to find.** Advanced mode's only distinguishing feature was an "Open classic queue" link. Two bugs stacked: the `classic=1` query param it generated was never read anywhere in `Admin_Menu_Handler::render_page()` (unlike the `tab=backups` special-case right next to where that check should have been), and even the classic page it would have reached (`admin/approvals.php`) had its batch-approve buttons (`agentic-approve-all-btn` etc.) with zero JS anywhere in the codebase wiring them up — dead on arrival, apparently orphaned when Approvals moved to React. Rather than resurrect that page, built real bulk approve/reject natively in the React view: `group_pending_for_bulk()` (`class-admin-pages-rest.php`) groups pending rows by agent + 120s window (same rule the old classic page used) and is included in `approvals_payload()` as `groups`; new `approval_decide_bulk` REST action processes a group server-side, scaffold/create actions first, stopping at the first failure for approve (reject has no such ordering need). Verified live with real inserted test rows: approve-all executed both items in DB-confirmed order, reject-all correctly skipped execution, `debug.log` clean.
  - **Table width.** All four screens (plus any future consumer of the shared `admin-pages`/`admin-list` React shell) were capped at the same `max-width: 1100px` as form-heavy pages like Settings, leaving a large empty strip on the right compared to classic `WP_List_Table` screens (e.g. Agents), which use the full content width. `AdminPage` (`src/shared/components.js`) now takes an opt-in `wide` prop (→ `.agentic-react-admin--wide { max-width: none; }`) — set by `admin-pages`/`admin-list`, left off `settings-app`/`dashboard-app` so those keep the narrower, more readable width.
  - **Operational gotcha hit while verifying this:** lffci.org sits behind Cloudflare, which edge-caches static assets (e.g. `assets/css/react-admin.css`) for 30 days keyed on the full URL including `?ver=`. A CSS-only deploy that doesn't bump `AGENT_BUILDER_VERSION` can look "not applied" in a real browser even though the file is correct on disk (hash-verified) and correct via `curl` with a cache-busting query param — Cloudflare is serving its own stale cached response for the *unchanged* `?ver=X.Y.Z` URL. No plugin-level Cloudflare integration exists on this site to purge it via WP-CLI; the practical fix is that the version bump step of the deploy workflow already solves this (new `?ver=` → cache miss) — but don't be fooled mid-session into thinking an asset-only fix didn't take when testing against an unbumped version. To force a real check before bumping, swap the stylesheet's `href` client-side (e.g. via the browser JS console) to a cache-busted URL rather than trusting a normal reload, even a hard one — the browser's own cache isn't the problem, Cloudflare's is.
- **3.3.64 — menu items should never move based on Basic/Advanced.** After 3.3.63 made the per-screen switch far more visible, user feedback was that switching modes shouldn't move things in the WP admin sidebar at all — Basic/Advanced should only ever affect a page's own content. Skills already got this treatment in 3.3.58; audited `Admin_Menu_Handler::register()` and found exactly two remaining menu items still gated by the site-wide default: "Publish" (`agentic-deployment`) and "Activity" (`agentic-audit-log`), both hidden from the sidebar in Basic mode via a shared `$agentic_advanced_parent` slug (`'agent-builder'` in Advanced, `''` — hidden-but-URL-reachable — in Basic). Both now always register under `'agent-builder'` unconditionally; `advanced_parent_slug()` had no other callers so it was removed rather than left dead. Verified live: set the global default to Basic, confirmed both still show in the sidebar. This also happened to leave `agentic_ui_mode` back at `basic`, its value from before any testing this session. No other menu items are gated by mode — this closes out that pattern entirely, so nothing needs the same treatment later.

### What just happened: Skills feature overhaul (3.3.57–3.3.61)

Skills (SKILL.md instruction bundles that teach agents workflows) had a complete data layer and admin UI since v2.5.2 but were **never actually wired into agent runtime** — `Skills_Registry::get_for_agent()` was dead code. This got fixed and substantially built out:

- **3.3.57** — Spec-aligned with [agentskills.io](https://agentskills.io) (the open standard Anthropic and Google's Android Skills both use): `allowed-tools` frontmatter, `license`/`compatibility`/`metadata` fields, spec validation. Wired skills into the actual prompt pipeline via progressive disclosure (name+description always in context; full body loaded on demand via a new `load_skill` tool — mirrors the existing OKF knowledge-wiki pattern). Bundled skills now auto-refresh from disk on plugin updates if never user-edited (`source_hash` tracking + a proper versioned schema migration — this surfaced and fixed a real pre-existing bug: `dbDelta()` can't parse `CREATE TABLE IF NOT EXISTS`, so this whole class of schema migration had likely been silently failing plugin-wide). Added 4 new bundled skills: `woocommerce-catalog-management`, `woocommerce-order-management`, `wp-content-authoring`, `gutenberg-blocks`.
- **3.3.58** — Skills now always shows in the admin menu (was hidden entirely in Basic mode); the Basic/Advanced split moved onto the page itself instead.
- **3.3.59** — Browse Community Skills now supports **3 sources** with a switcher: `github.com/WordPress/agent-skills` (official WordPress org repo, Basic mode default — this is the reputable, human-reviewed alternative to ClawHub's open-publish model), `github.com/anthropics/skills` (Advanced only), and OpenClaw/ClawHub (Advanced only, pre-existing). Fixed two real bugs found along the way: the import payload was `sanitize_text_field()`'d as a whole JSON blob before decoding (corrupts content with `<...>` in it — routine in WordPress examples), and cross-source dedup matched on slug alone (two registries' same-named skill could clobber each other).
- **3.3.60** — Two bugs caught by actually testing in a browser rather than trusting server-side checks: a CSS utility class's `display:flex` was silently defeating the native `hidden` attribute on the ClawHub-only search box; YAML block-scalar descriptions (`description: |-` — Anthropic's own `claude-api` skill uses this) were parsed as literally just the block indicator.
- **3.3.61** — The Basic/Advanced split from 3.3.58 only applied to the classic-PHP Skills page. The list view you actually land on from the nav renders through a **separate React component** (`SkillsView` in `src/admin-pages/index.js`, fed by `skills_payload()` in `class-admin-pages-rest.php`) that never got the same treatment. Fixed to match.

## Deploy workflow

reproduce → root-cause → fix → lint → version bump → deploy to lffci.org → verify live → check `debug.log` → commit → push. See global memory `feedback_deploy_workflow.md` for the full mechanics (scp to `/tmp`, `sudo cp` into place, `chown www-data:www-data`, hash-compare local vs remote before committing).

Live plugin path on lffci.org: `/var/www/lffci.org/public/wp-content/plugins/agent-builder` (note: directory is `agent-builder`, **not** `agent-builder-wp`). WP-CLI: `ssh lffci "sudo -u www-data wp --path=/var/www/lffci.org/public <command>"`.

**React admin surfaces** (`src/*/index.js`) need `npm run build` (wp-scripts) before deploying — the built output in `build/` is git-tracked and must be committed alongside source changes, not just the source.

**Bundled agent `abilities.json` edits** need a re-sign after deploying, or the agent gets blocked outright: `\Agentic\Abilities_Manifest::save_integrity_hash('<slug>')` via `wp eval`. The check is an HMAC against a server-local `abilities-signature` file (not in git, not in any DB table) — a content change with no matching re-sign fails closed with "Manifest signature mismatch" (audit log: `integrity_failure`). Discovered the hard way in 3.3.70.

## Key architecture notes

- **Basic/Advanced UI mode**: `Admin_Menu_Handler::is_advanced_mode( string $screen = '' )`, backed by the `agentic_ui_mode` option (global) with optional per-screen user-meta overrides. Established pattern for REST-fed React views: include `'is_advanced' => $is_advanced` in the payload (see `tools_payload()`/`approvals_payload()`/`logs_payload()`/`skills_payload()` in `class-admin-pages-rest.php`), consume as `data.is_advanced` in the corresponding `XxxView` component. For classic-PHP admin pages, call `is_advanced_mode('screen-name')` directly and gate markup — but remember to keep any hidden-in-Basic form field as a `<input type="hidden">` fallback so its value still round-trips on submit rather than getting silently reset.
- **Skills vs Tools vs Abilities**: Skills (`Skills_Registry`, `library/skills/*/SKILL.md`) are reusable instruction bundles teaching *how* to sequence tool calls — loaded into agent context via progressive disclosure. Tools (`Tools_Registry`, `library/tools/*/tool.php`) are the actual callable functions. Abilities (`Abilities_Manifest`, `abilities.json` per agent) are the separate *security/risk-tier* boundary — skills' `allowed-tools` is advisory scoping only, never a second permission system. Risk is normally declared per tool-name (flat `"risk"` key), but since 3.3.71 a tool whose actions genuinely differ in risk (a `list`/`get` read vs. `create`/`update`/`delete` writes) can add a `"risk_by_action": {"list": "none"}`-style map alongside the flat `risk` — `Abilities_Manifest::get_effective_risk()` takes an optional `$call_action` and checks that map first. Only narrows which of the tool's *own* actions the flat risk applies to; still can't go below the tool's own intrinsic default (`wp_agentic_tools.risk_level` / `get_risk_level()`), so the "manifest can only escalate, never downgrade" invariant holds exactly as before.
- **Mirror commits**: this repo receives periodic "mirror" commits from a separate private upstream, authored by "Agent Builder Release <support@agentic-plugin.com>", messages like "Release free (WPorg) vX.Y.Z" / "Mirror from private agent-builder". These are not something to create manually, aren't suspicious, and shouldn't be reverted/reconciled against local work — they're an established part of the workflow. Check `git log` for a newer such commit than what's live on lffci.org at the start of a session.
- **i18n**: fully built out as of 3.3.46 (11 locales, PHP `.po`/`.mo` + per-handle JS JSON). If new webpack entries or translatable strings are added, the `.pot` needs regenerating with `wp i18n make-pot . languages/agent-builder.pot --slug=agent-builder --exclude=build` (must exclude `build/` or webpack output pollutes references), and JS translations need the `make-json` output manually renamed from per-source-file to per-*handle* `{domain}-{locale}-{handle}.json` — WordPress's `load_script_textdomain()` won't find the default hashed filenames.
